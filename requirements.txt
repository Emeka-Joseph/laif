# Direct dependencies, pinned to the versions the site was built and tested
# against. Their own dependencies are deliberately left unpinned: pip resolves
# those against whichever Python the server provides, which is how the same
# file works on cPanel's Python 3.9 and on a newer interpreter.
#
# To lock the exact set on the server, run this there after a good install:
#     pip freeze > requirements.lock.txt
# A lock file has to be generated on the machine it is for.
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
PyMySQL==1.1.1

# Re-encodes uploaded photographs for the web as they arrive. Ships as a
# ready-built wheel for Python 3.9, so nothing is compiled on the server.
# If the install fails, uploads still work — they are just stored full size.
Pillow==11.3.0
