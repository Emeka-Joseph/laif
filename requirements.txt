# Pinned for reproducible installs on the server. These are the versions the
# site was built and tested against.
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
PyMySQL==1.1.1

# Resolved dependencies, pinned so a later release cannot change behaviour
# between a local test and the server.
SQLAlchemy==2.0.52
Werkzeug==3.1.8
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.5.0
blinker==1.9.0
greenlet==3.5.5
